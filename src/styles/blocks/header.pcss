.header{
    height: 90px;
    position: absolute;
    top: 0;
    width: 100%;
}
.logo{
    color: #ffffff;
    font-size: 36px;
    font-weight: 800;
    line-height: 30px;
    text-transform: uppercase;
}
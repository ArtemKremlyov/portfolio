.navbar__list{
    display: flex;
}
.navbar__item{
    font-size: 16px;
    font-weight: 400;
    line-height: 30px;
    margin-right: 60px;

     &:last-child{
         margin-right: 0;
     }
     a{
         &:hover{
             text-decoration: underline;
         }
     }
}